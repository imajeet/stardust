## 💫 ʇsnpɹɐʇs uᴉ sn ʎɹɹɐɔ

* Start VM - `vagrant up`, or execute `./provision.sh` if you already have one running.

Alternatively,

* Run dev by going to the /client - `npm i && npm start`
* Start /server - `npm i && node index.js`